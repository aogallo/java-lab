# Ejemplo tutor - Primer commit

```bash
git init
git status
git add .
git commit -m "feat: create academic system project"
```

El tutor muestra que el commit guarda el primer `Main.java` del sistema academico.
